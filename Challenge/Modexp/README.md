# MODEXP challenge: audit map

## Minimal specification

[`Spec.lean`](Spec.lean) defines the successful Osaka/EIP-7823 domain and the
raw-bytecode acceptance predicate `Challenge.Modexp.Correct`.
[`YulSpec.lean`](YulSpec.lean) defines its source-level counterpart,
`Challenge.Modexp.Yul.Correct`. The expected bytes in both are built from the
same padded EIP-198 parser, arbitrary-precision `modPow`, and fixed-width
encoder used by the pinned `Precompile.runModexp` semantics. These are the two
outer-facing predicates for auditors and candidate proofs; compiler transport
and the reference implementation are outside their functional definitions.

The three declared operand lengths must each be at most 1024 bytes. Oversized
tuples fail exceptionally under EIP-7823 and do not have a return value; the
reference executes `INVALID` for that separate failure domain.

## Reference artifact

[`Reference/reference.yul`](Reference/reference.yul) has a `MULMOD` fast path
for moduli up to 32 bytes and a 32-limb fallback for the complete 1024-byte
domain. [`Reference/reference.hex`](Reference/reference.hex) is the frozen
1,284-byte output of the pinned verified Yul compiler.

## Proof structure

[`ProofSupport/Bytecode.lean`](ProofSupport/Bytecode.lean) reduces the challenge
to `EvmProof.EventuallyEvaluates`. The direct proof is organized under
`Reference/Proofs/Bytecode/`; its execution and gas certificates target the
frozen byte array, not a compiler assumption.

[`Reference/Proofs/Bytecode/ReferenceCorrect.lean`](Reference/Proofs/Bytecode/ReferenceCorrect.lean)
composes every public branch and exports:

* `reference_correct : Correct referenceBytecode`, the end-to-end functional
  correctness theorem;
* `referenceGas`, the exact branch-sensitive gas formula; and
* `gasSteps_reference_cost`, proving the certified EVM trace consumes exactly
  `referenceGas input` for every valid input.

The multi-limb gas theorem telescopes setup, modulus scan, base conversion,
exponentiation, and serialization through the shared `EvmProof` memory-cost
potential. Functional correctness separately proves the limb arithmetic,
modular exponent invariant, and exact fixed-width output bytes.

### Source-Yul proof boundary

The reference also has a direct proof against the relational Yul semantics.
Reusable source-proof infrastructure lives under `Challenge/YulProof/`:

* `ClosedEvmDialect.lean` fixes the pure closed EVM source semantics, while
  `ClosedEvm.lean` adds executability and compiler transport support;
* `Bytes.lean` supplies pure `List UInt8`/`ByteArray` boundary lemmas;
* `Interpreter.lean` projects successful interpreter runs into relational
  big-step judgments;
* `EvmState.lean` supplies generic memory transformers and frame lemmas; and
* `Word.lean`, `Limbs.lean`, and `NatDigits.lean` supply fixed-width
  serialization, word-array representations, and positional-digit facts.

The MODEXP-specific proof is organized under `Reference/Proofs/Yul/`.
`Program` is the readable source AST, `StateModel` and `Procedures` give exact
source-state contracts, `Word`/`WordMath` prove the at-most-32-byte path, and
the remaining `Big*` modules prove source execution and arithmetic correctness
for the arbitrary-precision limb path. `Execution` composes the header dispatch
and every return branch into an unconditional source theorem.
`Reference/Proofs/Yul.lean` separately pins parsing, normalization,
optimization, compilation, and assembly to the frozen bytecode.

The direct source proof can be checked with:

```sh
lake build Challenge.Modexp.Reference.Proofs.Yul.Execution
lake build Challenge.Modexp.Reference.Proofs.Yul
```

## Falsification

```sh
lake exe modexpchallenge
lake exe modexpchallenge --yul=Challenge/Modexp/Reference/reference.yul \
  --fuzz=256 --seed=20260821
```

The executable suite includes the canonical EIP-198 vectors, zero-length and
zero-modulus cases, missing trailing bytes, and a 257-bit modulus exercising
the multi-limb implementation. The second command compiles the Yul source and
runs 256 reproducible generated cases in addition to those public vectors.
Tests are evidence, not the correctness proof.

<!-- BEGIN GENERATED MODEXP GAS REPORT -->
## Gas leaderboard

Generated by `scripts/report-modexp-gas.sh`; CI checks that this section is current.

### Category 1: Tier-1 measured gas

Gas is measured by concrete execution in the pinned EVM semantics.
The total covers all 9 public vectors, all of which are checked.
The Reference Yul row compiles the linked source with the pinned verified
compiler, requires its output to equal the frozen artifact, and scores
those compiled bytes.
Ratios compare suite totals against the pinned Osaka/EIP-7883 MODEXP
precompile and against the bundled reference. These measurements are tests,
not proofs.

| implementation | bytes | empty | small | zero exp | zero mod | zero M | EIP-198 #1 | 257-bit M | all vectors | vs precompile | vs reference |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| [Reference Yul](Reference/reference.yul) | 1284 | 183 | 2403 | 1193 | 950 | 183 | 39913 | 18958693 | 19046904 | 1633.53× | 1.00× |

### Category 2: proved calldata-dependent gas bounds

A proved row supplies a symbolic `gasFormula`, its executable `gasSchedule`,
and a kernel-checked `CorrectWithSchedule bytecode gasSchedule` theorem.
The checker requires `gasSchedule = gasFormula.eval` definitionally and
renders the checked syntax tree directly. Here `B`, `E`, and `M` are the
three decoded byte lengths, `V_M` is the padded modulus integer, and
$C_{\mathrm{mem}}(w)=3w+\lfloor w^2/512\rfloor$.
Rows are ordered by the proved schedules summed over the public vector suite;
this benchmark order is not a claim of global dominance over every tuple.

| implementation | proved symbolic bound | proof |
|---|---|---|
| [Reference Yul](Reference/reference.yul) | $`G(I) = \begin{cases}183,&M=0\\\\\begin{cases}\begin{cases}950,&V_M=0\\\\1053 + 140 \cdot B + 1210 \cdot E,&V_M\ne0\end{cases},&M\le32\\\\\begin{cases}253 + 343 + \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot 284 + M \cdot 190 + 56 + \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot 74 + C_{\mathrm{mem}}\left(192 + \left\lfloor\frac{M + 31}{32}\right\rfloor\right),&V_M=0\\\\253 + 343 + \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot 284 + M \cdot 190 + 50 + \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot 74 + 77 + \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot 71 + B \cdot \left(3506 + \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot 7248\right) + 206 + \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot 453 + 3 + E \cdot \left(106 + 8 \cdot \left(613 + \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot 526 + 2 \cdot \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot \left(102 + 256 \cdot \left(426 + \left\lfloor\frac{M + 31}{32}\right\rfloor \cdot 906\right)\right)\right)\right) + 66 + M \cdot 138 + C_{\mathrm{mem}}\left(192 + \left\lfloor\frac{M + 31}{32}\right\rfloor\right),&V_M\ne0\end{cases},&M>32\end{cases},&M\ne0\end{cases}`$ | [proved](Reference/Proofs/Gas.lean) |

<!-- END GENERATED MODEXP GAS REPORT -->
